# 📋 Task Plan: เพิ่มเคสทดสอบ 9211-9222/2569 สำหรับทดสอบหน้า approval-review.html

> **Plan ID:** `2026-09-16-add-demo-cases-9211-9222`
> **Date:** 2026-09-16
> **Author / Agent:** Claude Code
> **Status:** Done (implemented + CI ผ่าน — commit แล้ว, ยังไม่ push)
> **Branch / PR:** `main`

---

## 🎯 1. Problem Statement & Business Objective

- **ความต้องการ:** ผู้ใช้ต้องการชุดเคสสาธิต/ทดสอบเพิ่มเติม 12 เคส (`9211/2569` ถึง `9222/2569`) สร้างต่อจากบล็อกเคสทดสอบ
  `9200s` เดิม (`9201-9210/2569`) ในไฟล์ `assets/ecmis-app.js` array `CASES` เพื่อใช้ทดสอบหน้า `approval-review.html`
  ให้ครบทั้งสองสาย (7.1 ไต่สวนเบื้องต้น / 7.2 วินิจฉัยชี้มูล) และทั้งกรณีด่วน/ไม่ด่วน
- **ขอบเขต:** เป็นการเพิ่มข้อมูล mock/seed เท่านั้น (data-only) — ไม่แตะ logic, ไม่แตะ HTML, ไม่ต้อง `npm run sync`
- **แม่แบบอ้างอิง:** `9203/2569` (7.2 ไม่ด่วน), `9204/2569` (7.2 ด่วน), `9206-9210/2569` (7.1 ผสมด่วน/ไม่ด่วน)

---

## 📂 2. Affected Routes & Modules

- [x] Assets JS: `assets/ecmis-app.js` — เพิ่ม 12 case object ใน array `CASES` ต่อจากบล็อก 9200s เดิม (หลัง `9210/2569`
  ก่อน `0921/2569`)
- [x] ไม่แตะไฟล์ HTML (Root/`res/`) — ไม่ต้อง sync
- [x] ไม่แตะ logic/functions ใดๆ ใน `ecmis-app.js`

---

## ⛔ 3. 6 Golden Anti-Regression Rules — Pre-check

1. [x] ไม่แตะคอลัมน์ "ประเภทเรื่อง" ในตาราง inbox — ไม่เกี่ยวข้องกับงานนี้ (data-only)
2. [x] ไม่เปิด `agenda-registry.html` ให้ `chairman`/`affairs` — ไม่เกี่ยวข้อง
3. [x] ไม่เรียก `supabase.createClient()` ตรง — ไม่เกี่ยวข้อง (ไม่แตะโค้ด logic)
4. [x] Root/`res/` sync — ไม่เกี่ยวข้อง (ไม่แตะ HTML)
5. [x] A4 Geometry — ไม่เกี่ยวข้อง
6. [x] ไม่ใช้ `--no-verify` ตอน commit

---

## 🧩 4. Spec รายละเอียด 12 เคสใหม่

- `9211-9216/2569`: `procType:'7.1'`, `status:'PENDING_SECGEN'`, `docType:'213'`, `signPhase:'WAIT'`,
  `legalBase` สลับ `ม.18/4` / `ม.62`, 3 ด่วน (`urgent:true` + `urgentReason`) + 3 ไม่ด่วน (`urgent:false`)
- `9217-9222/2569`: `procType:'7.2'`, `status:'PENDING_SECGEN_72'`, `docType:'644'`, `legalBase:'ม.24 วรรคท้าย'`,
  3 ด่วน (`urgent:true, urgent72:true` + `urgentReason`) + 3 ไม่ด่วน (`urgent:false, urgent72:false`) —
  ไม่มี `meetingNo`/`agendaNo` (ยืนยันจาก 9203/9204 ที่ไม่มีฟิลด์นี้)
- Field set อื่นทั้งหมดตรงตามเคสอ้างอิง 100% (`owner, ownerOrg, complainant, accused[], allegation, receivedDate,
  deadline60, deadline2y, prescription, docRef, complex(/72), dupWarning, slaDays, slaLimit, subCommittee:null`)
- เนื้อหาเป็นเรื่องแต่งทั้งหมด: หน่วยงาน/จังหวัด/ผู้ถูกกล่าวหา/ข้อกล่าวหาไม่ซ้ำกับเคสเดิม (เทศบาลนคร, อบจ.,
  โยธาธิการฯ, ประมง, ปศุสัตว์, เกษตร, หน่วยป้องกันรักษาป่า, สรรพากรพื้นที่, จัดหางาน, การประปาฯ, การไฟฟ้าฯ,
  พัฒนาสังคมฯ)
- `receivedDate` กระจายในช่วง ม.ค.-มิ.ย. 2569, `deadline60` = +60 วัน, `deadline2y` = +2 ปี (คำนวณตรงตาม pattern เดิม),
  `prescription`: กรณีไม่ด่วน = receivedDate +3 ปี (ตาม pattern 9207-9210), กรณีด่วน = ใกล้ปัจจุบัน (~27-44 วัน)
  พร้อม `urgentReason` ระบุจำนวนวันที่ใกล้ครบกำหนดให้สอดคล้องกัน

---

## ✅ 5. Verification Matrix

- [x] `npm test` (5-layer CI): Syntax / Dual-Route / Zero-404 / Anti-Regression / A4 Layout — ทั้งหมดผ่าน
- [x] ตรวจ ID ซ้ำ: ไม่มี ID ซ้ำในไฟล์ทั้งหมด, มีครบ 12 ID ใหม่
- [x] ตรวจ field shape เทียบเคสอ้างอิง (9203/2569, 9206/2569, ตัวอย่างสุ่ม 4 เคสใหม่): key ตรงกันทั้งหมด
  (ผลต่างมีเฉพาะ `urgentReason` ซึ่งถูกต้องตามที่ควรมี/ไม่มีตามสถานะ urgent)

---

## 📝 6. Summary (เสร็จงานวันที่ 2026-09-16)

เพิ่มเคสทดสอบ 12 เคสใหม่ใน `assets/ecmis-app.js` ต่อจากบล็อก `9201-9210/2569` เดิม:

**7.1 ไต่สวนเบื้องต้น (PENDING_SECGEN):**
1. `9211/2569` — ด่วน — เจ้าหน้าที่เทศบาลนครเรียกรับเงินออกใบอนุญาตก่อสร้างอาคารสูง
2. `9212/2569` — ด่วน — เจ้าหน้าที่ อบจ. ฮั้วประมูลจัดซื้อครุภัณฑ์การแพทย์
3. `9213/2569` — ด่วน — เจ้าหน้าที่โยธาธิการและผังเมืองจังหวัดเรียกรับเงินออกใบอนุญาตจัดสรรที่ดิน
4. `9214/2569` — ไม่ด่วน — เจ้าหน้าที่ประมงจังหวัดยักยอกเงินอุดหนุนเกษตรกร
5. `9215/2569` — ไม่ด่วน — เจ้าหน้าที่ปศุสัตว์จังหวัดออกใบรับรองฟาร์มเท็จ
6. `9216/2569` — ไม่ด่วน — เจ้าหน้าที่เกษตรจังหวัดเรียกรับเงินอนุมัติสิทธิ์ปุ๋ยอุดหนุน

**7.2 วินิจฉัยชี้มูล (PENDING_SECGEN_72):**
7. `9217/2569` — ด่วน — หัวหน้าหน่วยป้องกันรักษาป่าเรียกรับเงินแลกใบอนุญาตทำไม้
8. `9218/2569` — ด่วน — เจ้าหน้าที่สรรพากรพื้นที่เรียกรับเงินแลกลดหย่อนภาษี
9. `9219/2569` — ด่วน — เจ้าหน้าที่จัดหางานจังหวัดเรียกค่าธรรมเนียมใบอนุญาตทำงานเกินอัตรา
10. `9220/2569` — ไม่ด่วน — เจ้าหน้าที่การประปาส่วนภูมิภาคฮั้วประมูลจัดซื้อท่อประปา
11. `9221/2569` — ไม่ด่วน — เจ้าหน้าที่การไฟฟ้าส่วนภูมิภาคยักยอกเงินค่าไฟฟ้า
12. `9222/2569` — ไม่ด่วน — เจ้าหน้าที่พัฒนาสังคมฯ จังหวัดยักยอกเงินสงเคราะห์ผู้ยากไร้

`npm test` ผ่านครบ 5 ด่าน (0 errors, 0 warnings). ไม่มี ID ซ้ำ. Field shape ตรวจสอบตรงกับเคสอ้างอิงทั้งสองสาย.
